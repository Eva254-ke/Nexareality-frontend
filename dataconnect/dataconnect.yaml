specVersion: "v1"
serviceId: "nexareality"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "nexareality-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
